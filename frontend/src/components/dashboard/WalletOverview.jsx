const WalletOverview = ({ data = [] }) => {
    return (
        <div className="p-4 bg-white rounded shadow">
            Wallet Overview (connect with Recharts later)
        </div>
    );
};

export default WalletOverview;