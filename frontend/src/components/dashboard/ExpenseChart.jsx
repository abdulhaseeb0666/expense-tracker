const ExpenseChart = () => {
    return (
        <div className="p-4 bg-white rounded shadow">
            Expense Chart (connect with Recharts later)
        </div>
    );
};

export default ExpenseChart;